import React from 'react';

const AnalyticsCharts = () => {
  // This component is now deprecated and its content moved to AnalyticsPage.
  // Returning null to remove it from the dashboard.
  return null;
};

export default AnalyticsCharts;
